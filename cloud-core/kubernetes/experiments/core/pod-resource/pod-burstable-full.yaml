apiVersion: v1
kind: Pod
metadata:
  name: pod-f
spec:
  containers:
  - name: nginx
    image: nginx:1.13
    resources:
      requests:
        cpu: 500m
        memory: 128Mi
      limits:
        cpu: 1
        memory: 1Gi
