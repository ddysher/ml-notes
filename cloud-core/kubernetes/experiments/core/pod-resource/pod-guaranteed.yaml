apiVersion: v1
kind: Pod
metadata:
  name: pod-g
spec:
  containers:
  - name: nginx
    image: nginx:1.13
    resources:
      requests:
        cpu: 500m
        memory: 128Mi
      limits:
        cpu: 500m
        memory: 128Mi
