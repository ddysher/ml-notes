apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    kubeless: kafka
  name: datadir-kafka-0
  namespace: kubeless
spec:
  storageClassName: standard
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
